# connection {{{
host = "localhost"
port = 6600

password = "user"
# }}}

# interface {{{
enable-mouse = no

library-page-tags = albumartist album disc

# included in screen-switch'g with (shift-)tab
screen-list = library browse

# search in mode |artist+title| by default
search-mode = 4

audible-bell = no
visible-bell = no

find-show-last = no
find-wrap = yes
wrap-around = no
bell-on-wrap = no

seek-time = 1.0
# }}}

# display {{{
welcome-screen-list = no

# NOTE:
#   1. do not specify |search-format| to continue using |list-format|
list-format = "%disc%.%track%  %title%"

# show length of item in separate column
second-column = yes

# do NOT (horizontally) scroll
scroll = no

# only highlight portion(s) of current line with text
wide-cursor = no
hardware-cursor = no
hide-cursor = 3.7

# status-line {{{
status-format = "%title%"

status-message-time = 2

timedisplay-type = elapsed

visible-bitrate = yes
# }}}

# misc {{{
text-editor = nvim

lyrics-autosave = no
# }}}
# }}}

# colors {{{
# NOTE:
#   000 := black
#   008 := grey
#   015 := white

enable-colors = yes

color background = 000

# title-lines {{{
# first row, LHS
color title-bold = 008
# foreground only for RHS, background for entire first row
color title = 008/000

# separator between title-lines & main display
color line = 008
# second row, RHS
color line-flags = 008
# }}}

# main display {{{
# non-highlighted
color list = 015

# highlighted (e.g., selected or currectly playing)
color list-bold = 012,bold/000
# }}}

# browser {{{
color browser-directory = 015
color browser-playlist = 015
# }}}

# status-line {{{
color progressbar = 000/000
color progressbar-background = 008

# play/pause indicator
color status-state = 008/000
color status-song  = 008/000
# time&bit-rate
color status-time  = 008/000

color alert = 000,bold/008
# }}}
# }}}

# vim: filetype=conf foldmethod=marker
